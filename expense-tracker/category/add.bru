meta {
  name: add
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/category
  body: json
  auth: inherit
}

body:json {
  {
    "category": "test1"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
